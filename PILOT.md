# Controlled Pilot Plan

- Run `GET /api/health` and `npm run test:hosted` before every pilot window.
- Start with one landlord, a small tenant cohort, and a limited number of active vendor dispatches.
- Watch for login failures, OTP failures, media access failures, and dispatch workflow stalls.
- Re-run hosted regression after every production deploy.
