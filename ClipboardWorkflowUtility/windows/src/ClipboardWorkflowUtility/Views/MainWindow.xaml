<Window x:Class="ClipboardWorkflowUtility.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Clipboard Workflow Utility"
        Height="640"
        Width="900">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <CheckBox Content="Pause capture"
                      IsChecked="{Binding CapturePaused}"
                      VerticalAlignment="Center"
                      Margin="0,0,16,0" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,8,0" />
            <Button Content="Clear All" Command="{Binding ClearCommand}" />
        </StackPanel>

        <ListView ItemsSource="{Binding HistoryItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" BorderThickness="0,0,0,1" BorderBrush="#22000000">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button Content="Copy"
                                        Margin="8,0,8,0"
                                        Command="{Binding DataContext.CopyItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" MaxHeight="60" />
                                <TextBlock Text="{Binding CreatedAtUtc}" FontSize="11" Foreground="Gray" />
                            </StackPanel>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</Window>
