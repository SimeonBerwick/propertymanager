# ── Database ──────────────────────────────────────────────────────────────────
# Local development uses PostgreSQL.
# Create a local database (for example, `property_manager`) and point BOTH vars
# at it unless you are using a pooled production connection.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/property_manager"
DATABASE_DIRECT_URL="postgresql://USER:PASSWORD@localhost:5432/property_manager"
#
# Production (Neon Postgres — pgbouncer pooled):
#   DATABASE_URL        → pooled connection string (Prisma Client runtime)
#   DATABASE_DIRECT_URL → non-pooled connection string (migrations / direct admin)
# If NOT using Neon's pooler, both values can be the same URL.
#
# DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-yyy.us-east-2.aws.neon.tech/neondb?pgbouncer=true&connection_limit=1&pool_timeout=0&sslmode=require"
# DATABASE_DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx-yyy.us-east-2.aws.neon.tech/neondb?sslmode=require"
#
# Optional integration-test database for `npm run test:authz`.
# Use a dedicated disposable database, not your main local dev DB.
# TEST_DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/property_manager_test"
# TEST_DATABASE_DIRECT_URL="postgresql://USER:PASSWORD@localhost:5432/property_manager_test"

# ── Auth ──────────────────────────────────────────────────────────────────────
# Required in production. Must be >= 32 chars and NOT the demo placeholder.
# Generate with: openssl rand -base64 48
# Local development can boot with the built-in demo fallback, but set a real
# value before sharing the app or testing signed-session behavior seriously.
AUTH_SECRET="change-this-demo-secret-before-sharing"

# ── App URL ───────────────────────────────────────────────────────────────────
# Used to build absolute invite links delivered to tenants.
# Must use HTTPS in production. Invite tokens are single-use and expire in 7 days.
# Local dev:
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Production:
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# ── Storage ───────────────────────────────────────────────────────────────────
# STORAGE_PROVIDER=local  -> files written to public/uploads/ (dev only)
# STORAGE_PROVIDER=r2     -> files stored in Cloudflare R2 (REQUIRED for production)
#
# WARNING: local storage uses the server's filesystem, which is ephemeral on
# platforms like Vercel. Any upload will be lost on the next deployment.
# Always use STORAGE_PROVIDER=r2 in production.
STORAGE_PROVIDER=local

# Cloudflare R2 -- required when STORAGE_PROVIDER=r2
# R2_ACCOUNT_ID=""
# R2_BUCKET=""
# R2_ACCESS_KEY_ID=""
# R2_SECRET_ACCESS_KEY=""

# ── Node environment ──────────────────────────────────────────────────────────
# Set to "production" in Vercel / hosting environment. Controls:
#   - cookie secure flag
#   - AUTH_SECRET demo-fallback guard
#   - OTP dev-mode code display (suppressed in production)
#   - local storage guard (storagePut throws if STORAGE_PROVIDER=local in prod)
NODE_ENV=development

# ── Seed / demo data ──────────────────────────────────────────────────────────
# SEED_ALLOWED=false prevents `prisma db seed` from wiping data in production.
# The seed script checks NODE_ENV=production + SEED_ALLOWED != "true" and exits.
# Never set SEED_ALLOWED=true in a production environment.
SEED_ALLOWED=false

# ── OTP transport ─────────────────────────────────────────────────────────────
# Tenant mobile OTP codes are delivered via SMS (phone channel) or email (email channel).
# In development, if provider env vars are absent the transport no-ops and the code
# is shown in-browser (NODE_ENV != production only — never shown in production).
# In production, BOTH sets of vars below must be set before enabling tenant mobile access.
# Missing vars cause an explicit startup-time throw, not a silent failure.
#
# SMS delivery via Twilio (required for tenants with a phone number):
# TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# TWILIO_AUTH_TOKEN="your_auth_token"
# TWILIO_FROM_NUMBER="+15005550006"   # E.164 format, e.g. a Twilio purchased number
#
# Email delivery via Resend (required for tenants with only an email address):
# RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# RESEND_FROM_ADDRESS="noreply@yourdomain.com"  # must be a verified Resend sender domain

# ── Optional ──────────────────────────────────────────────────────────────────
# LOG_LEVEL=info
# SENTRY_DSN=""
# SENTRY_ENVIRONMENT=""
