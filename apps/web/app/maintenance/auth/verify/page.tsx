import { StaffVerifyForm } from './form'
export default async function StaffVerifyPage({ searchParams }: { searchParams: Promise<{ challengeId?: string; masked?: string; devCode?: string }> }) { const query = await searchParams; return <div className="card stack" style={{ maxWidth: 560, margin: '48px auto' }}><div><div className="kicker">Maintenance staff</div><h1>Enter your code</h1></div><p className="muted">We sent a one-time code to {query.masked ?? 'your work email'}.</p>{query.devCode && process.env.NODE_ENV !== 'production' ? <div className="notice">Development code: <strong>{query.devCode}</strong></div> : null}{query.challengeId ? <StaffVerifyForm challengeId={query.challengeId} /> : <a href="/maintenance/auth/login">Start over</a>}</div> }
